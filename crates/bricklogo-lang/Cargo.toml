[package]
name = "bricklogo-lang"
version = "0.1.0"
edition = "2024"
description = "Logo language interpreter for BrickLogo"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[dev-dependencies]
